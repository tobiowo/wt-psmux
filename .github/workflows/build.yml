name: Build

on:
  push:
    branches: [master]
  pull_request:
  schedule:
    # Check weekly if upstream terminal has new releases
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  check-patch:
    name: Verify patch applies cleanly
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Apply patches
        shell: pwsh
        run: ./apply-patches.ps1

  build:
    name: Build (${{ matrix.platform }})
    needs: check-patch
    runs-on: windows-latest
    strategy:
      matrix:
        platform: [x64, arm64]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Apply patches
        shell: pwsh
        run: ./apply-patches.ps1

      - name: Detect Windows SDK version
        shell: pwsh
        run: |
          $sdk = (Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\Include" |
            Where-Object { $_.Name -match '10\.0\.\d+' } |
            Sort-Object Name -Descending |
            Select-Object -First 1).Name
          Write-Host "Using Windows SDK: $sdk"
          "WIN_SDK=$sdk" >> $env:GITHUB_ENV

      - name: Restore NuGet packages
        working-directory: terminal
        shell: pwsh
        run: |
          # Use WT's own bundled nuget.exe (same as Invoke-OpenConsoleBuild in tools/OpenConsole.psm1)
          & .\dep\nuget\nuget.exe restore OpenConsole.slnx
          & .\dep\nuget\nuget.exe restore dep\nuget\packages.config

      - name: Build
        working-directory: terminal
        run: |
          # Build specific targets via the solution so MSBuild resolves the full
          # WinRT code-gen dependency chain without building the MSIX packaging
          # project (CascadiaPackage.wapproj) which requires signing certs.
          # Target names derived from project paths: \ -> _, extension dropped.
          msbuild OpenConsole.slnx /restore `
            /t:src_cascadia_WindowsTerminal_WindowsTerminal`;src_cascadia_wt_wt `
            /p:Platform=${{ matrix.platform }} `
            /p:Configuration=Release `
            /p:WindowsTargetPlatformVersion=${{ env.WIN_SDK }} `
            /m /v:minimal

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wt-psmux-${{ matrix.platform }}
          path: |
            terminal/${{ matrix.platform }}/Release/WindowsTerminal.exe
            terminal/${{ matrix.platform }}/Release/wt.exe

  release:
    name: Create release
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true  # need terminal submodule to read its version tag

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get terminal version
        id: version
        run: |
          tag=$(git -C terminal describe --tags --abbrev=0 2>/dev/null || echo "unknown")
          echo "tag=$tag" >> $GITHUB_OUTPUT

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}-psmux
          name: ${{ steps.version.outputs.tag }} + psmux patches
          body: |
            Patched build of Windows Terminal ${{ steps.version.outputs.tag }}.

            **Changes over upstream:**
            - `wt close-tab [-t <index>]` — close a tab by index (or the active tab)
            - `wt rename-tab <title>` — rename a tab by sending a title to the active tab

            **Usage:**
            Drop `WindowsTerminal.exe` and `wt.exe` into your Windows Terminal installation directory.
          files: artifacts/**/*.exe
